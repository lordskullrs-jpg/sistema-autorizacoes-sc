import DashboardMonitor from './DashboardMonitor';

// Admin tem as mesmas funcionalidades do Monitor por enquanto
// Pode ser expandido no futuro com funcionalidades administrativas
export default function DashboardAdmin() {
  return <DashboardMonitor />;
}
